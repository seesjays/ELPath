from dearpygui.core import *
from dearpygui.simple import *

with window("weird buttons or normal?", width=880, height=880):
    add_button("lmao")
    add_spacing(count=1)
    add_button("testing")
    add_spacing(count=1)
    add_button("demo")
    add_spacing(count=1)
    add_button("a longer button")
    add_spacing(count=1)
    add_button("Lmao indeed")
    add_spacing(count=1)
    add_button("lmao2")
start_dearpygui()
